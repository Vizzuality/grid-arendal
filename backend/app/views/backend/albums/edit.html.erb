<div class="l-items-index">
  <%= render 'backend/shared/media_contents/media_contents', key: "albums", items: @albums %>
</div>

<div class="l-items-detail">
  <%= simple_form_for @album, html: { class: 'c-form' } do |f| %>
    <%= render 'form',
        form: f,
        album_photos: @album_photos,
        tags: @tags
    %>
  <% end %>
</div>
