$(function() {
  $('.l-items-detail').html('<%= escape_javascript(
    render 'form',
    publication: @publication,
    users: @users,
    partners: @partners,
    have_picture: false,
    picture_url: nil
  ) %>');
  var formView = new App.View.Form({});
});
