<div class="l-items-index">
  <h2>Users</h2>

  <ul>
   <% @users.each do |user| %>
      <li>
        <%= link_to "#{user.full_name} (#{user.status})", user_path(user) %>
        <%= user.email %>
        <%= link_to 'Edit', edit_user_path(user) %>
      </li>
    <% end %>
  </ul>
</div>
