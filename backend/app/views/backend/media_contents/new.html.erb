<div class="l-navigation">
  <%= render 'backend/shared/nav_bar' %>
</div>

<div class="l-items-index">
  <%= render 'media_contents', albums: @albums, photos: @photos %>
</div>

<div class="l-items-detail">
  <%= simple_form_for @media_content, html: { class: 'c-form' } do |f| %>
    <%= render 'form',
      form: f,
      have_picture: false,
      picture_url: nil %>
  <% end %>
</div>
