<div class="l-items-index">
  <%= render 'activities', activities: @activities %>
</div>

<div class="l-items-detail">
  <%= simple_form_for @activity do |f| %>
    <%= render 'form', form: f, section: 'new', users: @users, partners: @partners,
      news_articles: @news_articles, publications: @publications %>
  <% end %>
</div>
