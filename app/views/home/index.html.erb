<div class="l-header-home">
  <div class="overlay"></div>
  <div class="row">
    <div class="small-12 columns">
      <div class="c-events-modal" style="background-image: url('');">
        <svg class="icon icon-share">
          <use xlink:href="#icon-close"></use>
        </svg>
        <div class="event-info">
          <h3 class="text -title-single-alt">It's our world.</h3>
          <h3 class="text -title-single-alt">Let's take action.</h3>
          <p class="text -body-single">Our staff is currently at UNEA-2 in Kenya. Join us in the discussion about the critical environmental challenges facing the world today.</p>
          <div class="btn -dark">
            Find out more
          </div>
        </div>
        <div class="event-logo"></div>
      </div>
    </div>
  </div>
  <%= render 'shared/menu' %>
  <div class="cta">
    <div class="logo">
      <svg class="icon">
        <use xlink:href="#icon-logo"></use>
      </svg>
    </div>
    <div class="cta-desktop text -cta">
      GRID-Arendal works closely with the United Nations Environment Programme (UNEP),
      other UN agencies and partners around the world to connect science to policy.<br>
      Our goal is to reduce the time between when new knowledge is produced and when it is used.<br>
      We believe it’s important influence thinking and action at the global level
      because our greatest challenges require collective action.
    </div>
    <div class="media">
      <svg class="icon">
        <use xlink:href="#icon-play"></use>
      </svg>
    </div>
  </div>
</div>

<div class="cta-mobile text -cta">
  GRID-Arendal works closely with the United Nations Environment Programme (UNEP),
  other UN agencies and partners around the world to connect science to policy.
  Our goal is to reduce the time between when new knowledge is produced and when it is used.
  We believe it’s important influence thinking and action at the global level
  because our greatest challenges require collective action.
</div>

<% if @event %>
  <div>
    <h3><%= @event.title %></h3>
    <%= @event.description %>
    <br />
    <%= link_to "Learn more", @event.web_url %>
    <% if @event.partner %>
      <p>sponsored by <%= link_to @event.partner.name, @event.partner.web_url %></p>
    <% end %>
  </div>
<% end %>

<%= render 'shared/news' %>

<%= render 'shared/posts', collection: @activities, title: 'activities', link: activities_path %>

<%= render 'shared/posts', collection: @publications, title: 'publications', link: publications_path %>

<%= render 'shared/media' %>

<%= render 'shared/users' %>

<%= render 'shared/footer' %>
