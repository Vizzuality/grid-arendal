<h2>Our staff</h2>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= image_tag(user.avatar) %>
      <%= link_to user.display_name, staff_path(user) %>
      <%= user.current_position %>
    </li>
  <% end %>
</ul>
