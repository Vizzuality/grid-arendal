<% if @activities.any? %>
  $(function(){
    salvattore.appendElements(document.querySelector('.masonry-layout'), $('<div/>')
      .html("<%= escape_javascript(render("related_posts", collection: @activities )) %>")
      .contents()
    );
  });
<% end %>
