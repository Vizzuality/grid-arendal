$(function() {
  $(".masonry-layout").find(".item").remove();
  var grid = document.querySelector('.masonry-layout');
  salvattore.appendElements(grid, $('<div/>').
    html("<%= escape_javascript(render("publications", publications: @publications)) %>").
    contents());
});
