<div class="filter -type-years js-filter" data-filter-type="years" data-filter-key="<%= key %>">
  <ul class="list">
    <% data.each do |year| %>
      <li class="text -filter-year" data-value="<%= year %>"><%= year %></li>
    <% end %>
  </ul>
</div>
