<% if @activities.any? %>
  $(function(){
    salvattore.appendElements(document.querySelector('.masonry-layout'), $('<div/>')
      .html("<%= escape_javascript(render("activities", activities: @activities )) %>")
      .contents()
    );
  });
<% end %>
