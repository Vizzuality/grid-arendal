<div class="l-header-single -activities-article" <% if @activity.picture_file_name %>style="background-image: url('<%= @activity.picture %>');"<% end %>>
  <div class="overlay"></div>
  <%= render 'shared/menu' %>
  <div class="row">
    <div class="small-12 columns">
      <div class="title-block">
        <div class="c-breadcrumbs">
          <a href="/">
            <svg class="icon-home">
              <use xlink:href="#icon-breadcrumb-home"></use>
            </svg>
          </a>
          <svg class="icon">
            <use xlink:href="#icon-breadcrumb-chevron-right"></use>
          </svg>
          <%= link_to "Activities", activities_path, class: "text -meta" %>
          <svg class="icon">
            <use xlink:href="#icon-breadcrumb-chevron-right"></use>
          </svg>
          <span class="text -meta"><%= @activity.title %></span>
        </div>
      </div>
    </div>
  </div>

  <div class="section-menu">
    <div class="row">
      <div class="small-12 columns">
        <div class="title-block">
          <div class="small-8 medium-7">
            <h2 class="text -title-single-alt"><%= @activity.title %></h2>
          </div>
          <div class="share">
            <svg class="icon">
              <use xlink:href="#icon-share"></use>
            </svg>
          </div>
        </div>

        <ul class="anchors">
          <li class="text -section-menu js-anchor" data-section-belongs="overview">Overview</li>
          <li class="text -section-menu js-anchor" data-section-belongs="users">Staff involved</li>
          <li class="text -section-menu js-anchor" data-section-belongs="posts">Publications</li>
          <li class="text -section-menu js-anchor" data-section-belongs="news">Related News</li>
          <li class="text -section-menu js-anchor" data-section-belongs="media">Related media</li>
          <li class="text -section-menu js-anchor" data-section-belongs="partners">Partners & Links</li>
        </ul>
      </div>
    </div>
  </div>
</div>

<section class="l-single-article -activities" data-section="overview">

  <div class="row">
    <div class="small-12 column">
      <div class="text -summary-alt"><%= raw @activity.description %></div>
    </div>
  </div>
</section>

<%= render 'shared/users' %>

<%= render 'shared/posts', collection: @publications, title: 'publications', link: publications_path %>

<%= render 'shared/news' %>

<%= render 'shared/media' %>

<%= render 'shared/partners' %>

<%= render 'shared/footer' %>
