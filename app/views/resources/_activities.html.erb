<% if media_content.activities.try(:any?) %>
  <%= render 'shared/posts', collection: media_content.activities.published.
    order(:title),
    title: 'Related activities',
    link_txt: 'View all activities',
    link: activities_path, sectionClass: 'content -color-4' %>
<% end %>
